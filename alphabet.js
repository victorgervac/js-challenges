//Assume numbers and punctuation symbols will not be included in the string.

let word =["vowels", "alphabet","zero", "wind", "elbow"];
//Have the function alphabetSoup(str) 
function alphabetSoup(){
     //method arrange in largest to smallest sort.()
    return word.sort();
//take the str string parameter being passed 
// return the string with the letters in alphabetical order (ie. hello becomes ehllo). 
}
console.log(alphabetSoup("hello"));